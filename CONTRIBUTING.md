# CONTRIBUTING

Build incrementally (phases 1–9). Keep `npm run build` green. Strict TypeScript, no `as any`. Parameterized SQL only. Label every mock. Never bypass DRM, auth, or paywalls. Never commit secrets — use .env.example.
